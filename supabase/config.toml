# Minimal Supabase configuration required by Supabase CLI for remote operations
# (e.g. CLI link, migrations push/pull, Edge Functions deployment)
# Local development emulator sections have been removed.

project_id = "promptpolish"
